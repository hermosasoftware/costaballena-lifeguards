# "Help us reopen Playa Hermosa" campaign (home page section).
# Public values embedded in the HTML at build time — safe to expose to the client.
# Copy to `.env` or set the same variables in your deployment platform.

PUBLIC_PLAYA_HERMOSA_RAISED=60000
PUBLIC_PLAYA_HERMOSA_GOAL=100000
